import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faCamera } from '@fortawesome/free-solid-svg-icons'

const Camera = () => {
  return (
    <span>
     <FontAwesomeIcon icon={faCamera} className='h1'/>
    </span>
  )
}

export default Camera